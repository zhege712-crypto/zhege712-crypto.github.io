{
  "name": "san-umbrella-site",
  "private": true,
  "version": "1.0.0",
  "description": "伞 / 视觉作品集 — 静态个人站(GitHub Pages)",
  "scripts": {
    "dev": "vite --port 7100 --host"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
